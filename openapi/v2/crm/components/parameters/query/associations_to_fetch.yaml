name: associations_to_fetch
in: query
schema:
  type: array
  items:
    type: string
    example: contact
description: |
  List of any additional associated objects to attempt to fetch associations for (on top of the associations that are already part of the common model). Only relevant for hubspot and when `read_from_cache` is false.
