anyOf:
  - type: object
    properties:
      type:
        type: string
        enum:
          - standard
          - custom
      name:
        type: string
        example: object_name
    required:
      - type
      - name
  - type: object
    properties:
      type:
        type: string
        enum:
          - common
      name:
        type: string
        enum:
          - contact
          - account
          - opportunity
          - user
          - lead
          - sequence
          - mailbox
          - sequence_state
    required:
      - type
      - name
