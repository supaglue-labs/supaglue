name: expand
in: query
schema:
  type: array
  items:
    type: string
    example: account
description: |
  If set, will hydrate any associated objects of the specified type. Only supported for Hubspot and Salesforce.
