type: object
properties:
  category:
    $ref: ./objects/category.yaml
  auth_type:
    type: string
    enum:
      - oauth2
  provider_name:
    $ref: ./objects/provider_name.yaml
  config:
    $ref: ./objects/integration_config.yaml
required:
  - category
  - auth_type
  - provider_name
  - config
