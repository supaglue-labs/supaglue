{
  "name": "@supaglue/types",
  "version": "0.1.1-1",
  "repository": {
    "type": "git",
    "url": "https://github.com/supaglue-labs/supaglue.git",
    "directory": "packages/types"
  },
  "main": "./src/index.ts",
  "peerDependencies": {
    "typescript": "^4.9.4"
  },
  "scripts": {
    "build": "yarn run clean && yarn run -T tsc -p .",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "publish": "run -T turbo run build --filter=@supaglue/types ... && yarn npm publish",
    "lint-staged": "run -T lint-staged --no-stash --quiet",
    "lint": "run -T eslint"
  },
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "files": [
    "dist"
  ]
}
